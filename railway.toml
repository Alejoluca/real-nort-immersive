[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server/src/realtime-hub.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 8
